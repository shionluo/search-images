// Import
import styled from 'styled-components';

// ----------------------------------------------------------------------------------------- //

export const ImageContainer = styled.div`
  :hover {
    cursor: pointer;
    transform: scale(1.2);
    transition: transform 2s;
  }
`;
